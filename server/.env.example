PORT=4000
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
OPENAI_API_KEY=
GHL_WEBHOOK_SECRET=change-me
# 32-byte key as 64 hex chars; generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
# Cron schedule for the daily scan (default 08:00 server time)
CRON_SCHEDULE=0 8 * * *
